# ⚠️ DEPRECATED — DO NOT USE ⚠️
# =============================================================================
# The Helm chart for nanobot-factory VDP-2026 is DEPRECATED as of 2026-06-24.
# All deployments now use bare-metal systemd (see deploy/bare_metal/).
#
# These templates are retained for archaeology / reference only. Do NOT
# `helm install` or `helm upgrade` from this directory.
# =============================================================================

# (Templates intact for diffing; will be removed in P4 cleanup pass.)