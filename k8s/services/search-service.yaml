# =====================================================================
# search-service (port 8011) - P3-3-W2 (uses pgvector)
# =====================================================================

apiVersion: v1
kind: ServiceAccount
metadata:
  name: search-service
  namespace: nanobot-factory
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: nanobot-factory
  labels:
    app: search-service
    app.kubernetes.io/component: microservice
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
        app.kubernetes.io/component: microservice
    spec:
      serviceAccountName: search-service
      containers:
        - name: search-service
          image: ghcr.io/minimax-ai/nanobot-factory:v0.8.0
          imagePullPolicy: IfNotPresent
          command:
            - /opt/venv/bin/uvicorn
            - backend.services.search_service.main:app
            - --host
            - 0.0.0.0
            - --port
            - "8011"
            - --log-level
            - info
          ports:
            - name: http
              containerPort: 8011
              protocol: TCP
          env:
            - name: NANOBOT_PORT
              value: "8011"
            - name: PG_DSN
              valueFrom:
                configMapKeyRef:
                  name: nanobot-config
                  key: PG_DSN
            - name: SEARCH_SERVICE_URL
              value: http://search-service.nanobot-factory.svc.cluster.local:8011
          envFrom:
            - configMapRef:
                name: nanobot-config
          resources:
            requests:
              cpu: 300m
              memory: 512Mi
            limits:
              cpu: "1.5"
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8011
            initialDelaySeconds: 25
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8011
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: nanobot-factory
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8011
      targetPort: 8011
      protocol: TCP
  selector:
    app: search-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: search-service
  namespace: nanobot-factory
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: search-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 30
      policies:
        - type: Percent
          value: 100
          periodSeconds: 30
    scaleDown:
      stabilizationWindowSeconds: 300