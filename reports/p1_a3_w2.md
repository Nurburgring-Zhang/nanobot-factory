# P1-A3-W2 Report — 工作流节点契约校验 + 众包结算

## TL;DR
- ✅ 26 用例 (≥ 22 要求) 全部 PASS, 0.30 秒
- ✅ 5 个新文件, 0 修改既有文件
- ✅ 引擎层 (engines/) 与 API 层 (api/) 解耦, 可独立测试

## 工时
- 启动 + 探索 (conftest/api/engines 路径冲突): ~6 min
- 引擎实现 (ContractValidator + CrowdSettlementEngine): ~8 min
- API 路由 (workflow_contract + crowd_settlement): ~4 min
- 测试套件 + 调试 (sys.path 冲突 + withdraw_history 重名 + test 逻辑): ~8 min
- 报告: ~2 min

## 关键决策
1. **引擎纯内存**: 不用 SQLite (与既有 routes 区分), 加速测试 10x+ (0.3s vs 3s)
2. **API 文件命名不带 _routes**: 避免与既有 `api/*_routes.py` 冲突, 同时让用户/集成测试明确区分
3. **sys.path 修复策略**: 复用 R2-W5 系列手法, 在测试模块级 `sys.path[:] = ...` 过滤掉 `backend/`, 把 `imdf/` 置顶

## 任务覆盖矩阵

| 任务项 | 完成情况 | 验证 |
|--------|----------|------|
| `engines/contract_validator.py` 新建 | ✅ | 244 行, 7 endpoints 路由层用 |
| `engines/crowd_settlement.py` 新建 | ✅ | 264 行, 7 endpoints 路由层用 |
| `api/workflow_contract.py` 重写 | ✅ (新建) | 7 endpoints |
| `api/crowd_settlement.py` 重写 | ✅ (新建) | 7 endpoints |
| `tests/test_p1_a3_contract_crowd.py` ≥ 22 用例 | ✅ 26 用例 | `pytest` 26 passed |
| `reports/p1_a3_w2.md` | ✅ | (本文件) |

## 关键路径修复
详见 `deliverable.md` 第 2 节。简言之: pytest rootdir 是 `backend/`, 而 `backend/api/` 与 `imdf/api/` 同名冲突, 需要在测试里把 `imdf/` 强制置顶到 sys.path[0]。

## 回归
- 26 个新测试全 PASS
- 32 个既有 R2 validator 测试无回归
- 合并跑 58 passed in 0.37s

## 后续建议
- 如需把 P1-A3 新版契约校验端点接到主 app, 在 `canvas_web.py` 添加 `include_router` (见 deliverable.md 第 3 节)
- 引擎可演进到 SQLite 持久化以支持多进程, 当前 InMemory 已能满足单 worker 场景
- `validate_workflow` 可扩展 sub-graph / conditional edges 支持