# R5-Worker-1 报告 — settings.js 8 个死按钮接 API

**任务**: R5-Worker-1 (2026-06-18)
**作者**: coder (Mavis agent)
**会话**: mvs_e042daebc47044939d0a4c58ae59081a
**父会话**: mvs_91baf17749dd4f6d8186cb3b56fbf925
**状态**: ✅ DONE — 8/8 按钮接 API 验证通过

---

## 1. TL;DR

| 按钮 (settings.js) | HTTP | 端点 | 状态 |
|---|---|---|---|
| `saveAPISettings` (L267) | POST | `/api/settings/api` | **新建** |
| `saveModelSettings` (L285) | POST | `/api/settings/models` | **新建** |
| `saveStorageSettings` (L310) | POST | `/api/settings/storage` | **新建** |
| `saveNotificationSettings` (L333) | POST | `/api/settings/notifications` | **新建** |
| `testAPIConnection` (L373) | GET | `/api/v1/health` | 已存在 |
| `generateNewApiKey` (L386) | POST | `/api/v1/api-keys/create` | 已存在 |
| `clearCache` (L408) | POST | `/api/settings/cache/clear` | **新建** |
| `checkForUpdates` (L421) | GET | `/api/v1/health` | 已存在 |

**结果**: 5 个新后端端点 (9 routes 含 GET) + 3 个已存在端点验证可达, **8/8 按钮接 API 全部真触发后端, 无静默**。

---

## 2. 死按钮清单 (修复前)

| # | 按钮 | 端点 (前端已写) | 后端 (修复前) | 修复 |
|---|---|---|---|---|
| 1 | 保存 API 配置 | `POST /api/settings/api` | ❌ 404 | ✅ 新建 settings_routes.py |
| 2 | 保存模型设置 | `POST /api/settings/models` | ❌ 404 | ✅ 新建 |
| 3 | 保存存储配置 | `POST /api/settings/storage` | ❌ 404 | ✅ 新建 |
| 4 | 保存通知设置 | `POST /api/settings/notifications` | ❌ 404 | ✅ 新建 |
| 5 | 测试连接 | `GET /api/v1/health` | ✅ 已存在 | ✅ 验证可达 |
| 6 | 生成新 Key | `POST /api/v1/api-keys/create` | ✅ 已存在 | ✅ 验证可达 (需 auth) |
| 7 | 清除缓存 | `POST /api/settings/cache/clear` | ❌ 404 | ✅ 新建 |
| 8 | 检查更新 | `GET /api/v1/health` | ✅ 已存在 | ✅ 验证可达 |

> 修复前用户点 1-4 / 7 共 5 个按钮 → 404 + toast 错误, 5/8 是真死按钮 (3/8 后端已有但前端 OK)。

---

## 3. 修改文件

### 3.1 新建 `backend/imdf/api/settings_routes.py` (388 行)

**职责**: 处理前端 settings.js 5 个新端点 + 4 个 GET 读取端点。

**核心组件**:
- 4 套 Pydantic v2 BaseModel: `APISettings` / `ModelSettings` / `StorageSettings` / `NotificationSettings`
- 7 个工具函数: `_ensure_dir` / `_load_preferences` / `_save_preferences` / `_update_section` + 3 个 field_validator
- 9 routes: 5 POST + 4 GET, 全部 prefix `/api/settings`, tags=["settings"]

**关键代码片段**:

```python
# Pydantic v2 model with field_validator (Pydantic v1 升级已统一)
class APISettings(BaseModel):
    base_url: str = Field(..., min_length=1, max_length=2000)
    timeout: int = Field(30, ge=1, le=300)
    max_retries: int = Field(3, ge=0, le=10)

    @field_validator("base_url")
    @classmethod
    def validate_url(cls, v: str) -> str:
        v = v.strip()
        if not re.match(r"^https?://[^\s/$.?#].[^\s]*$", v, re.IGNORECASE):
            raise ValueError("base_url 格式无效, 必须以 http:// 或 https:// 开头")
        return v


# 原子写入 — 防半写入损坏
def _save_preferences(prefs: Dict[str, Any]) -> None:
    _ensure_dir()
    tmp_path = USER_PREFERENCES_FILE.with_suffix(".json.tmp")
    try:
        with open(tmp_path, "w", encoding="utf-8") as f:
            json.dump(prefs, f, ensure_ascii=False, indent=2)
        os.replace(tmp_path, USER_PREFERENCES_FILE)  # 原子替换
    except OSError as e:
        if tmp_path.exists():
            try: tmp_path.unlink()
            except OSError: pass
        raise HTTPException(status_code=500, detail=f"保存失败: {e}")
```

### 3.2 修改 `backend/imdf/api/canvas_web.py` (+8 行, L2338-2344)

注册新 router (放在 API Key 路由之后, 沿用 try/except 模式, 失败不影响其他):

```python
# R5-Worker-1: settings.js 8 个死按钮接 API — /api/settings/{api,models,storage,notifications,cache/clear}
try:
    from api.settings_routes import router as settings_router
    app.include_router(settings_router)
    logger.info("设置页面路由已加载 (R5-W1: /api/settings/*)")
except Exception as e:
    logger.warning(f"设置页面路由加载失败: {e}")
```

### 3.3 运行时新建 `data/settings/user_preferences.json` (28 行)

首次 POST 后生成, **与 `system_config.py` 使用的 `data/settings.json` 隔离**, 互不干扰:

```json
{
  "api": { "base_url": "http://localhost:8000/api/v1", "timeout": 30, "max_retries": 3 },
  "models": { "default_llm": "deepseek-v4-pro", "embedding_model": "text-embedding-3-large", "vision_model": "qwen-vl-max", "temperature": 0.7, "max_tokens": 4096 },
  "storage": { "storage_type": "local", "storage_path": "/data/imdf/storage", "cache_size_gb": 10, "auto_backup": true },
  "notifications": { "task_complete": true, "delivery_review": true, "quality_alert": true, "system_alert": true, "email_notify": false, "quality_threshold": 85 }
}
```

### 3.4 前端 `frontend/imdf/js/pages/settings.js` — **无需修改**

`apiGet` / `apiPost` 已正确调用后端 (api.js L88-127 通用 API 调用层 + JWT 自动注入), 缺的只是后端端点。

---

## 4. 验证策略 (Pydantic v2 + 路径防护)

| 验证项 | 规则 | 测试输入 | 期望 |
|---|---|---|---|
| URL 格式 | `^https?://[^\s/$.?#].[^\s]*$` | `"not-a-url"` | 422 + "base_url 格式无效" |
| timeout 范围 | 1-300 | `999` | 422 + "less_than_equal 300" |
| max_retries 范围 | 0-10 | `15` | 422 + "less_than_equal 10" |
| temperature 范围 | 0.0-2.0 | `3.0` | 422 + "less_than_equal 2.0" |
| max_tokens 范围 | 1-128000 | `0` | 422 + "greater_than_equal 1" |
| storage_type 白名单 | local/s3/minio/oss/cos | `"Amazon S3"` | ✅ 200 (中文→s3 映射) |
| storage_path 系统目录 | 拒绝 /etc /proc /sys /dev /boot | `"/etc/passwd"` | 422 + "不允许使用系统目录" |
| storage_path 路径注入 | 拒绝 NUL /\n /\r | `"/data/x\0y"` | 422 + "包含非法字符" |
| cache_size_gb 范围 | 1-1000 | `0` | 422 + "greater_than_equal 1" |
| quality_threshold 范围 | 0-100 | `150` | 422 + "less_than_equal 100" |
| 完整往返 | save → get | `{...有效值...}` | 200 + GET 返回相同 |

**安全防护层**:
1. URL 格式正则 (前端 inputs[0].value 任意字符串)
2. 危险路径黑名单 (系统目录 + Windows 关键路径)
3. 字符串长度限制 (DoS 防护, max 100-2000 字符)
4. Pydantic v2 数字范围 (类型 + 范围双重校验)
5. 存储类型白名单 (含中文显示值映射, 兼容前端下拉框)

---

## 5. 验证结果 (8/8 PASS + 边界 11/11)

### 5.1 8 按钮真触发验证 (smoke test)

```
$ python C:\Users\Administrator\.mavis\plans\plan_ddd6f14b\workspace\smoke_test_r5w1.py
=== 8 buttons smoke test ===

[1] saveAPISettings -> POST /api/settings/api
    200 OK, success=True, message=API配置已保存
[2] saveModelSettings -> POST /api/settings/models
    200 OK, success=True, message=模型设置已保存
[3] saveStorageSettings -> POST /api/settings/storage
    200 OK, success=True, message=存储配置已保存
[4] saveNotificationSettings -> POST /api/settings/notifications
    200 OK, success=True, message=通知设置已保存
[5] testAPIConnection -> GET /api/v1/health (existing)
    200 OK, status=ok, version=2.0.0
[6] generateNewApiKey -> POST /api/v1/api-keys/create (existing, needs auth)
    Route registered, without auth: 401 (expected 401)
[7] clearCache -> POST /api/settings/cache/clear
    200 OK, files_cleared=0, bytes_mb=0.0, message=已清理 0 个文件, 0.0 MB
[8] checkForUpdates -> GET /api/v1/health (existing)
    200 OK, version=2.0.0

=== ALL 8 buttons verified ===
```

### 5.2 边界验证 (11/11)

| # | 测试 | 输入 | 期望 | 实际 |
|---|------|------|------|------|
| 1 | GET 空配置 | 无 | 200 + 默认值 | ✅ 200 |
| 2 | POST 有效 | `{base_url, timeout, max_retries}` 合法 | 200 + success | ✅ 200 |
| 3 | POST 无效 URL | `base_url="not-a-url"` | 422 | ✅ 422 + 中文错误信息 |
| 4 | POST 超界 timeout | `timeout=999` | 422 | ✅ 422 + "less_than_equal 300" |
| 5 | POST models 有效 | `{llm, embedding, vision, temp, tokens}` | 200 | ✅ 200 |
| 6 | POST storage 危险路径 | `storage_path="/etc/passwd"` | 422 | ✅ 422 + "不允许使用系统目录" |
| 7 | POST notifications 有效 | `{5 booleans + threshold}` | 200 | ✅ 200 |
| 8 | POST cache/clear | 无 | 200 + cleanup stats | ✅ 200 |
| 9 | GET 回读 | 读取持久化 | 200 + 一致 | ✅ 200 (与 POST 数据一致) |
| 10 | 持久化文件 | `data/settings/user_preferences.json` | 存在 + 4 section | ✅ 28 行 JSON |
| 11 | 原子写入 | 模拟断电中途 | tmp 文件清理 | ✅ 异常路径清理 |

---

## 6. 与既有 R1/R2 模式对齐

| 模式 | R1 实践 | R2 实践 | R5-W1 (本任务) |
|------|---------|---------|---------------|
| Pydantic | v1 `@validator` | v2 `field_validator` | ✅ v2 (R2 升级) |
| 错误响应 | `{"success": False, "error": "..."}` | 同 R1 | ✅ 同 |
| 路由前缀 | `/api/v1/...` 为主 | `/api/v1/...` 为主 | `/api/settings/...` (前端已定) |
| 持久化 | 散落 JSON 文件 | 同 R1 | ✅ `data/settings/user_preferences.json` (与 system_config 隔离) |
| 验证 | `_common/validators.py` | `_common/` 多模块 + body_schemas | ✅ 路由内 field_validator (避免循环 import) |
| 注册 | `app.include_router` 在 canvas_web.py | 同 R1 | ✅ try/except 块 (L2338-2344) |
| 测试 | pytest `tests/integration/` | 同 R1 | smoke test (workspace/, 无 pytest 依赖) |

**Pydantic v2 注意**: 不用 `req.dict()` (v1), 用 `req.model_dump()` (v2)。沿用 R2 升级后的项目标准。

---

## 7. 已知瑕疵 (不在本任务范围)

1. **前端 inputs 索引脆弱**: settings.js L270 / 312 / 315 用 `inputs[N]` 索引取值, 任意 input 顺序变化即错位。建议改为 `input[name="..."]` 或 `id` 选择器。**R6 UX 阶段处理**。

2. **`||` 默认值吞 0**: settings.js L272 `parseInt(inputs[3]?.value) || 3` — 用户输入 `0` 会被替换为 `3` (JS truthy 行为)。**R6 UX 阶段处理**。

3. **审计日志未写**: 当前 `_update_section` 写 JSON 但未写 `audit.db` 表 (与 R2.5 audit_routes 模式不同)。可选增强, **P1 不要求**。

4. **storage_type 中文映射**: 前端下拉框显示"本地文件系统" / "亚马逊 S3"等中文, 后端需做 mapping 兼容 (settings_routes.py L209-216)。如前端改为英文, 移除 mapping 即可。

---

## 8. 验证方法 (供 verifier 复核)

### 8.1 后端导入验证
```bash
cd "D:\Hermes\生产平台\nanobot-factory\backend\imdf"
python -c "import sys; sys.path.insert(0, '.'); from api.settings_routes import router; print(len(router.routes), 'routes')"
# 预期: 9 routes
```

### 8.2 端点可达性 smoke test
```powershell
$env:JWT_SECRET = "test-secret-32chars-long-string-aaaaaaaa"
cd "D:\Hermes\生产平台\nanobot-factory\backend\imdf"
python "C:\Users\Administrator\.mavis\plans\plan_ddd6f14b\workspace\smoke_test_r5w1.py"
# 预期: "=== ALL 8 buttons verified ==="
```

### 8.3 浏览器手动验证 (plan.yaml verify_prompt)
1. 登录 IMDF → 点击"系统设置"
2. 切换 5 个 tab (API / 模型 / 存储 / 通知 / 关于)
3. 依次点击 8 个按钮:
   - 保存配置 (4 个 tab 各 1 个)
   - 测试连接 / 生成新 Key / 清除缓存 / 检查更新
4. 全部按钮触发后端请求 (Network 面板可见 POST/GET), 不仅是 toast

### 8.4 持久化验证
```bash
# 修改前
type D:\Hermes\生产平台\nanobot-factory\backend\imdf\data\settings\user_preferences.json
# 应看到 4 section JSON, 修改 UI 后内容应实时更新
```

---

## 9. 总结

| 指标 | 数值 |
|------|------|
| 死按钮数 (修复前) | 5 (api/models/storage/notifications/cache) |
| 已存在但需验证 | 3 (health, api-keys, health) |
| 新建后端端点 | 5 POST + 4 GET (9 routes) |
| 新建文件 | 1 (settings_routes.py, 388 行) |
| 修改文件 | 1 (canvas_web.py, +8 行) |
| 持久化文件 | 1 (user_preferences.json, 28 行) |
| 前端修改 | 0 (代码本就正确) |
| smoke test | 8/8 PASS |
| 边界测试 | 11/11 PASS |
| 路由注册 | ✅ try/except 单点失败不影响其他 |
| Pydantic v2 | ✅ 沿用 R2 升级后标准 |
| 路径注入防护 | ✅ /etc /proc /sys /Windows 拒绝 |
| 原子写入 | ✅ tmp + os.replace |

**R5-Worker-1 完成, 等待 R5-Auditor-A/B/C 验证阶段。**
