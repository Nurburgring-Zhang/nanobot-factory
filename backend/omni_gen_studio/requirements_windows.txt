# AIGC批处理工具 v5.4 依赖包
# Python 3.8+ required
# Windows兼容版本

# 核心依赖 (Python内置，通常不需要pip安装)
# tkinter - GUI框架 (Python内置)
# threading - 多线程 (Python内置)
# json - JSON处理 (Python内置)
# os, sys - 系统操作 (Python内置)
# pathlib - 路径操作 (Python内置)

# 图像处理核心
pillow>=9.0.0
numpy>=1.21.0
opencv-python>=4.5.0
scikit-image>=0.19.0

# 网络请求
requests>=2.25.0

# 数据处理
pandas>=1.3.0
openpyxl>=3.0.0

# 机器学习基础
scikit-learn>=1.0.0
matplotlib>=3.4.0

# AI模型支持 (ComfyUI和WebUI必需)
torch>=2.0.0
torchvision>=0.15.0  
torchaudio>=2.0.0
diffusers>=0.21.0
transformers>=4.25.0
accelerate>=0.20.0
xformers>=0.0.20

# ComfyUI特定依赖
safetensors>=0.3.0
onnx>=1.12.0
onnxruntime>=1.12.0
omegaconf>=2.2.0
hydra-core>=1.2.0

# WebUI特定依赖 (AUTOMATIC1111)
gfpgan>=1.3.8
realesrgan>=0.3.0
clip-by-openai>=1.0
latent-consistency-model>=1.0
open-clip-torch>=2.0.0

# 视频处理
opencv-contrib-python>=4.5.0

# 3D处理 (可选)
# trimesh>=3.10.0

# 工具库
colorama>=0.4.4
tqdm>=4.62.0
certifi>=2023.7.22
charset-normalizer>=3.2.0
idna>=3.4
urllib3>=2.0.4

# 额外依赖
psutil>=5.9.0
chardet>=5.0.0
six>=1.16.0
setuptools>=68.0.0
wheel>=0.41.0

# Windows特定支持
# pywin32>=227  # Windows API支持 (可选)
# psutil>=5.8.0  # 系统信息 (可选)

# 性能优化 (可选)
# accelerate  # 用于GPU加速
# onnxruntime  # 用于ONNX推理加速

# 开发工具 (可选)
# pytest>=6.0.0  # 测试框架
# black>=21.0.0  # 代码格式化
# flake8>=4.0.0  # 代码检查