/**
 * R3.5-W2 stub: RunningHub 工具 Provider
 */
import type { FC, ReactNode } from 'react';

interface RHToolsProviderProps {
  children?: ReactNode;
}

export const RHToolsProvider: FC<RHToolsProviderProps> = ({ children }) => {
  return <>{children}</>;
};

export default RHToolsProvider;
