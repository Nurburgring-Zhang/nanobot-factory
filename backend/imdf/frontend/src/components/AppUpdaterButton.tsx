/**
 * R3.5-W2 stub: 应用更新按钮
 */
import type { FC } from 'react';

interface AppUpdaterButtonProps {
  isPixel?: boolean;
  isDark?: boolean;
}

const AppUpdaterButton: FC<AppUpdaterButtonProps> = () => null;

export default AppUpdaterButton;
