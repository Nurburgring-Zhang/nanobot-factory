"""Test package for :mod:`backend.imdf.agency`.

Imports nothing so the directory is treated as a package by pytest.
Kept intentionally minimal — see ``test_loader.py`` and ``test_departments.py``.
"""
